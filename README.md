```markdown
# Order Management System

This repository contains the Order Management System (Flask) that you downloaded from the chat.

Quick start:
- Install dependencies: `pip install -r requirements.txt`
- Initialize DB and seed users: `python init_db.py`
- Run: `python run.py`

Seeded users: admin@example.com/adminpass, cse@example.com/csepass, pe@example.com/pepass, customer@example.com/custpass, mgmt@example.com/mgmtpass
```